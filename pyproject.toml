[project]
name = "test_package"
version = "0.1.0"
description = "several python packages for automation"

[build-system]
requires = [
    "setuptools>=60"
]
build-backend = "setuptools.build_meta"

[tool.setuptools]
packages = ['package']